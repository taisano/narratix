# CLAUDE.md — Chart Advisor

## このプロジェクトは何か

Chart Advisor は、ビジネス資料向けのチャートを「言いたいこと」から設計し、編集できるPPTとして出力するWebアプリ。Google スプレッドシート版の NarratiX（目的起点のチャート作成ツール）の進化版で、次の3つの入り口を持つ。

- **Wish**：相談文から、AIが切り口（仮説／メッセージ）と表現を提案する
- **目的**：Trend / Comparison / Composition / Contribution / Relationship / Evaluate から選ぶ
- **Chart**：作りたいチャートが決まっている人がすぐ作る

利用者はコンサル・経営企画・マーケティングなど、PowerPoint で報告資料を作るビジネスパーソン。画面とスライドは日本語・英語に対応する。

## 最初に読むもの

1. `docs/registry-spec.md` … 設計書（正本）。エンティティ、スキーマ、20チャート、補完パーツ、8レイアウト、出力方式、多言語対応
2. `reference/mekko-builder.html` … 動く見本。2時点データ → Mekko＋揃えた成長率表 → 編集可能なPPTX。配置計算を1つにしてSVGプレビューとPPT出力で共有している
3. `docs/narratix-handoff.md` … 旧 NarratiX のコード棚卸し（既存の設定項目と未解決点）
4. `docs/layouts.pdf` … スライドのレイアウト8パターン

## 守る原則

- **レジストリが唯一の設計図**：目的・チャート・設定・補完パーツ・レイアウト・変換の定義は `registry` に置き、画面・描画・検証・保存・AI・出力はすべてそこを参照する。コンポーネントやエクスポーターにチャートごとのルールを直書きしない。
- **データは1つ、見せ方は複数**：Dataset と ViewSpec を分ける。ViewSpec は最初から `panels[]`（1〜4個）の形で持つ。最初の実装が1パネルでも保存形式は変えない。
- **値は実数で受け取り、率はアプリが計算する**：構成比・成長率・pt変化は transform で計算する。
- **プレビューとPPTは同じ配置計算から作る**：レイアウトはインチ単位（16:9 = 13.333 × 7.5 in）で1回だけ計算し、SVG描画とPPTX出力の両方がその結果を使う。標準の出力は「図形で組む（shapes）」。
- **AIは ViewSpec の JSON を返すだけ**：描画・計算・出力はプログラムが担う。AIの出力はレジストリで検証し、存在しないIDは弾く。AI呼び出しは Wish の入り口に限定する。
- **多言語は最初から**：画面の文言はコンポーネントに直書きせず翻訳ファイル（ja / en）から読む。レジストリの表示名は `LocalizedText`。スライドに自動で入る文言は `slideLocale` に従う。
- **NarratiX の挙動を推測で再実装しない**：計算・検証・インサイト・Advice の生成ルールはバックエンドのコードが届くまで保留し、仮実装には `// TODO(narratix): 要確認` を付ける。

## 技術スタック（2026-09-23 確定。理由は `docs/decisions.md`）

- 画面：Next.js 16（App Router）＋ TypeScript 5.9
- レジストリ：TypeScript の型付きファイル（`src/registry/`）。JSON Schema も生成できる形にする
- 描画：SVG（自前の配置エンジン）
- PPT出力：PptxGenJS 3.12.0（固定）
- テスト：Vitest（transform と配置計算は単体テストを必須にする）
- DB・認証：Supabase（Postgres＋Auth＋RLS）。課金（Stripe）は後から
- 後から：Wish の AI 呼び出し（サーバー側から。APIキーをクライアントに置かない）

## 最初のマイルストーン（M1）

Composition（Mekko）を端から端まで通す。

- Dataset（MEKKO スキーマ、現在＋比較期間）を表で入力できる。Excel からの貼り付けに対応
- ViewSpec（p01 と p05 レイアウト）から、Mekko・揃えた成長率表・左の合計100%積み上げをプレビューできる
- 補完パーツ aligned_table / delta_labels をオン・オフできる
- shapes 方式で PPTX を出力できる（見た目がプレビューと一致すること）
- 画面を日本語・英語で切り替えられる
- 完了の目安：`reference/mekko-builder.html` と同等以上の出力が、レジストリ経由で作れる

## 見本コードから得た実装上の注意

- PptxGenJS 3.12 では、テキストボックスの `margin` はポイント、**表のセルの `margin` はインチ**として扱われる（ポイントで渡すと文字が縦書きのように潰れる）。
- 表の列幅を Mekko の列幅に揃えるには、`addTable` の `colW` に [行ラベル幅, 各列幅…] を渡す。
- セグメント内の文字色は背景色の輝度で白と濃紺を切り替える。
- 成長率表の色の濃さは行ごとに正規化する（市場全体と形状別で桁が違うため）。

## 作業の進め方

- 新しいセッションでは、実装の前に計画（変更するファイル、手順、確認方法）を示して合意を取る。
- 設計書と食い違う判断をしたら、`docs/registry-spec.md` の該当箇所を更新するか、`docs/decisions.md` に理由を1〜3行で残す。
- ユーザーとのやり取りは日本語で行う。
