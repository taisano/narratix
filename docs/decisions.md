# 決定記録

設計書（registry-spec.md）と食い違う判断や、実装上の選択を1〜3行で残す。

## 2026-09-23

- **コードの置き場所**：iCloud Drive 外の `~/Project/Narratix web app` を git リポジトリの正本とする。iCloud 側の指示書フォルダは参照用に残し、以後の更新はリポジトリ内の `docs/` で行う。
- **技術スタック確定**：Next.js 16（App Router）＋ TypeScript 5.9、Vitest、zod 4（レジストリと ViewSpec の検証。JSON Schema は `z.toJSONSchema` で生成）、Supabase（Postgres＋Auth＋RLS）。TypeScript 7 は Next.js との組み合わせ実績が少ないため見送り。
- **PptxGenJS は 3.12.0 に固定**：見本コードの注意点（テキストの margin はpt、表セルの margin はインチ）が 3.12 の挙動に基づくため。4.x への更新は出力の見た目を検証してから。
- **ホスティング**：Vercel を想定（未確定）。

## 2026-09-23（レジストリ）

- **設定の適用先は Control 側だけに持つ**：チャート定義に `controls` 一覧は置かず、`controlsFor(chart)` で逆引きする（二重管理を避ける）。補完パーツは、チャート側に「推奨」、補完パーツ側に「適用可能（appliesTo）」を持ち、推奨 ⊆ 適用可能をテストで保証する。appliesTo は設計書の「主な適用チャート」と各チャートの「推奨補完」の和集合。
- **見せられる／見せられないは語彙（AspectId）で持つ**：設計書の日本語の説明を20語の語彙に置き換え、補完パーツの `covers` と突き合わせる。語彙は `src/registry/aspects.ts`。
- **スキーマの互換**：MATRIX 系チャート（trend / comparison）は MEKKO データも受け取れる（どちらも「行×列の実数」）。p05 の左の合計棒（stacked_100）を MEKKO データから描くため。transform を持つパネルはスキーマ照合を省き、変換後の形は配置エンジン側で確かめる。
- **パネル型の補完パーツの置き場所**：揃えた表は既定で p02（上下 3:1）の下、Mekko は p05 の右下、横棒ランキングとヒートマップは p03 の右（行揃え）。示唆ボックスは p03 の右（3:1）。
- **ViewSpec の id / version は任意**：AI が返す JSON には含まれないため。保存時に DB 側で付ける。
- **sparkline は保留**：3時点以上が必要だが、Dataset は current / base の2時点しか持てない。Dataset の拡張と合わせて決める。
- **slope は比較期間を必須にしない**：MATRIX_TIME_SERIES の2行（2時点）でも描けるため。

## 2026-09-23（transform と配置エンジン）

- **見本をゴールデン値にする**：`scripts/extract-reference-golden.mjs` が `reference/mekko-builder.html` の `model()` と `layout()` を Node でそのまま実行し、5パターン（標準、期間の伸び率、強調、表なし、入力順）の結果を `src/engine/__fixtures__/reference-mekko.json` に書き出す。エンジンはこれと全アイテムの座標・文言・色が一致することをテストで保証する。見本を変えたらスクリプトを再実行する。
- **見本の構成は「p02 上下（上：Mekko、下：揃えた表）」で再現する**：揃えの指定でつながったパネルの間隔は 0.14 in（通常は 0.25 in）。列で揃える表は、比率で決まる高さより内容が小さければ内容の高さに縮め、余りをチャート側に渡す（比率は上限として働く）。
- **スライド枠の寸法を見本に合わせた**：タイトル y=0.32、パネル領域 1.18〜6.90、出典 y=7.02（`SLIDE_FRAME`）。
- **Dataset に `dimensions`（行・列が何を表すか）を追加**：Mekko の注記「高さ：形状の構成比」に必要なため。未入力なら「セグメント」。
- **Mekko の左余白**：揃えた表がある、または左に y_scale で揃えるパネルがない場合は 1.75 in（見本と同じ）。左に合計棒がある p05 で表もない場合は 0.6 in。
- **描画を実装済みのチャートは mekko と stacked_100 のみ**（`IMPLEMENTED_CHARTS`）。他は `ComposeError('not_implemented')` を返す。

## 2026-09-23（プレビュー画面）

- **画面の多言語は自前の小さな仕組みにした**（`src/i18n/ui.tsx` の `useT()`）。next-intl は URL に言語を入れるルーティングが前提で、今の1画面構成には重いため外した。翻訳ファイル（messages/ja.json・en.json）の形は同じなので、必要になれば置き換えられる。日英のキーと差し込みが揃っていることはテストで確認する。
- **空きスロットは詰める**：パネルを置かないスロットは幅0・間隔0にする。Mekko ビルダーは常に p05 を使い、左の合計棒・下の表のオンオフは「そのスロットを空ける」で表す。
- **p05 で左に合計棒があるとき**、Mekko の左余白は下の表の行ラベルが入る幅まで詰め、「形状構成比（2025年）」の見出しは出さない（合計棒の見出しと重複するため）。
- **作業途中のデータはブラウザに保存**（localStorage）。ログインと保存は Supabase で後から入れる。
- **画面の状態 → ViewSpec**：画面は `BuilderState` を編集し、描画のたびに `toViewSpec()` で ViewSpec に変換してレジストリで検証してから描く。レイアウトと置き場所は補完パーツ aligned_table の定義から取る。

## 2026-09-23（PPTX 出力）

- **PPTX は Scene をそのまま置く**（`src/export/pptx/scene-to-pptx.ts`）。チャート固有のルールは持たない。図形・文字の位置と大きさが Scene と ±1 EMU で一致すること、揃えた表の列幅が Mekko の列と一致することを、生成した PPTX の XML を展開してテストする。
- **フォントはスライドの言語で決める**：日本語 Meiryo、英語 Arial（`SLIDE_FONTS`）。社内規定のフォントに変える設定は後から。
- **PptxGenJS はダウンロードボタンを押した時に読み込む**（画面の初回表示を軽くするため）。
- **元データのスライド**（付録）も Scene として配置する（`layoutDataSlide`）。行が多い場合のページ分けは未対応。
- **表の行ラベル列の余白を 0.4in に**：0.2in では PowerPoint／代替フォントで「市場全体 CAGR」が折り返した（LibreOffice で PPTX を画像にして確認）。
- **強調時の凡例は色のまま**（見本と同じ）。強調していない系列の凡例をグレーにするかは要検討。

## 2026-09-23（ログインと保存）

- **テーブル**：projects / datasets / view_specs / view_spec_versions。すべて owner_id = auth.uid() の RLS。anon には権限を与えない。履歴（versions）は追記のみ。
- **保存は RPC `save_chart` 1回**：データ・ViewSpec・履歴をまとめて書き、version を進める（security invoker なので RLS がそのまま効く）。新規保存で最初のプロジェクト「マイプロジェクト」を自動で作る（プロジェクトの画面はまだない）。
- **ViewSpec と画面の状態を両方保存する**：`spec` はレジストリで検証済みの ViewSpec（出力・AI の正本）、`ui` は開き直すための BuilderState。検証に通らない状態は保存しない。
- **削除は datasets を消す**：外部キーの cascade で ViewSpec と履歴も消える。
- **ログインはメールのリンク（PKCE）**：リンクから戻るとブラウザのクライアントが ?code= を自動で交換する。サーバー側で Supabase を使う処理はまだないので、proxy.ts（旧 middleware）とコールバック用のルートは置いていない。サーバー側で読む処理を足す時に入れる。
- **テスト**：migration は PGlite に Supabase の auth を模したスキーマを作って実行し、保存・版の進み・他人の行が見えない／書けない・未ログインで読めない・履歴を書き換えられない、を確かめる。画面の流れは Playwright で Supabase を差し替えて確認した（自動テストには入れていない）。

## 2026-09-24（依存関係の注意）

- **npm audit の high 2件は pptxgenjs → image-size**：画像を読み込む時の不具合（DoS）。このアプリは PPT に画像を入れないので該当しない。`npm audit fix --force` は pptxgenjs を 2.2 に戻して出力が壊れるため実行しない。画像を入れる機能を足す時に、pptxgenjs の更新と合わせて見直す。
- **Node.js は 22 か 24 の LTS**：vitest 5 が 23 を対象外にしている（`engines` に明記）。

## 2026-09-24（マイページとチャート名）

- **チャート名（name）をスライドのタイトル（title）と分けた**：一覧で見分けるための名前。初回保存と「別名で保存」で名前を聞く（初期値はスライドのタイトル／「○○ のコピー」）。上書き保存では聞かない。名前の変更は版を進めない。
- **migration は追記で足す**（`20260924000000_chart_name.sql`）。save_chart の引数が変わるため、関数を作り直している。Supabase 側で SQL を実行するまで、新しい画面からの保存は失敗する。
- **マイページ（/charts）**：縮小プレビュー（保存した画面の状態から、その場で配置・SVG を作る）、検索（名前・タイトル）、並び替え（更新・作成・名前）、開く・名前変更・複製・2段階の削除。一覧は1回の取得で全件（件数が増えたらページ分けを入れる）。
- **エディタとマイページの行き来は URL で渡す**（`/?chart=<id>`、`/?new=1`）。エディタに未保存の変更があれば、進む前に確認を出す（ブラウザの確認ダイアログは使わない）。
- **ヘッダー・ログイン・画面の言語は AppShell にまとめ**、layout.tsx で全画面に置く。共通のスタイルは `src/features/ui.module.css`。

## 2026-09-24（Trend・Comparison と行と列の入れ替え）

- **NarratiX のルールに合わせた点**（`docs/narratix-rules.md`）：比較は「行＝対象（多くは年）、列＝並べる項目」。対象の既定は最後の行、並び順は降順・昇順・入力順、空欄の列は除く。折れ線は空欄で途切れる。強調は対象を濃紺・他をグレー、強調した線を最前面に。CAGR は横軸が年のとき最初→最後の年。数値表記は formatDriverMetricValue_ の規則。既定値（目盛線・値ラベル OFF、マーカー ON）。
- **NarratiX から変えた点**：値の軸は 0 ちょうどから（下に余白を付けない）、マイナスの棒は 0 の線から下（左）へ、目盛はきりの良い刻み（1・2・2.5・5×10^n、区間 3〜7、端が値に最も近いもの）、数値に桁区切り。数値表記の既定は「そのまま」（NarratiX の自動 K/M は選択肢に残す）。
- **行と列の入れ替え（axis_swap）はデータを変えない**：描く直前に transpose を掛ける。Contribution 以外の全チャートで使える（NarratiX では Mekko・Evaluate は固定だった）。同じスライドの揃えたパネル（Mekko の左の合計棒・下の表）にも同じく掛ける。表示する行・列の絞り込みは入れ替えの前（入力した表の行・列）に対して行う。
- **設定の候補は入れ替え後の軸から**：強調＝列（系列）、比較の対象・基準・比較先＝行。レジストリの `dataSource` で持つ。候補にない値は ViewSpec に入れない（既定に戻る）。
- **配色**：推移・比較の系列は NarratiX の PROFESSIONAL_DISTINCT_PALETTE、構成系（Mekko・積み上げ・100% 積み上げ）は Mekko の見本の配色。
- **CAGR 注記は比較期間を使わない**（NarratiX と同じく横軸の年から計算）。レジストリの requiresBase を never に変えた。
- **保存形式 v2**：チャートの種類・設定・補完パーツを汎用に持つ。v1（Mekko 専用）は読み込み時に自動で変換し、次に保存した時に v2 で書き直される。
- **エディタのフォルダ名を editor に変更**（mekko-builder から）。ブラウザ保存のキーは互換のため据え置き。

## 2026-09-24 3つの入り口で「レシピ」を共通の単位にする
- 相談（AI）・目的・チャートのどの入り口でも、②で選ぶものは「レシピ」（メインのチャート＋付け合わせの表・補完パーツ＋説明）にそろえる。チャート1つだけのレシピもある。
- 入り口ごとに違うのは絞り込み方だけ：相談＝AI が最大3つ選ぶ（1回だけ）、目的＝その目的のレシピ、チャート＝そのチャートがメインのレシピ（＋別の目的の先頭レシピを提案）。目的・チャートからは AI を使わない。
- 並びは「先頭＝リード案、次の2つ＝サブ案、残り＝ほかの候補（折りたたみ）」。保存は選んだレシピの ID。
- 理由：組み合わせ（チャート＋表）をどの入り口からも出せる、説明文を1回書けば済む、②の画面が1種類で済む。詳しくは `docs/consultation-flow.md`。
- ②の絵はデータ入力前なので、名前・数値を入れない抽象図にする。

## 2026-09-24 推薦レシピの土台（src/registry/recipes.ts）
- **レシピはコードの中に置く**（Supabase ではなく）。チャート・補完パーツ・レイアウトと同じレジストリの一部なので、「存在しないチャートや設定を指すレシピ」はテストで弾ける。版は `RECIPE_DB_VERSION`（ViewSpec の `recipe.version` に残る）。
- **17レシピ**：推移8・比較5・構成4。推移・比較・構成のすべてのチャートに、チャート1つだけのレシピがある。ID は指示書に合わせて大文字（`TREND_CAGR_TABLE` など）。
- **見せられる／見えにくいは手で書かない**：チャートの shows / cannotShow、補完パーツと表の covers から組み立てる（`recipeAspects`）。レシピだけの注意は `extraCannotShow`。
- **見えにくいことへの案内**（`recipeRemedies`）：先にメインのチャートに足せる補完パーツ、無ければそれを見せられる別のレシピ（目的が重なるもの優先）。
- **相談からの並べ方は規則で決める**（`rankRecipes`・`RECIPE_SCORING`）：主な目的 +40／副の目的 +25／違う目的 −40、年が要るレシピは期間が書かれていれば +20（無ければ −20）、場面 +10、成長率・規模・正確な値が必要で見せられれば各 +10、読み取りが重い −10。違う切り口を並べるため、同じメインのチャートは1つまで、主な目的が重なると −15。40点未満は出さない（無理に3案目を作らない）。指示書の例（海外5地域・経営会議）で、指示書と同じ3案・同じ順になることをテストで固定。
- **AI の入出力の形**（`consultation.ts`）：分類（不明は null / 'unknown'）と recipe_id・reason_codes だけ。JSON Schema は `consultationJsonSchema()`。保存する推薦の状態は `ai_used_after_data_input: false` を型で固定。
- **データを入れた後の確認はエンジン側**（`checkRecipeData`・`recipeIssueText`）：データの形、数値の有無、行数、年（1900〜2100 が2つ以上）、開始年・終了年の欠け（指示書の定型文）、CAGR の N/A、比較期間、系列の多さ。AI は使わない。
- **Mekko のレシピは Mekko の形のデータが必要**（今の互換表どおり）。Mekko の形のデータなら推移・比較のレシピも描ける。
- **描けるかどうかは別に判定**（`recipeRenderable`）。今描けるのは9レシピ。CAGR 表（`cagr_table`）、集合縦棒、100%横棒、差分バー、スロープはこれから作る部品。
- 足したもの：表 `cagr_table`、変換 `endpoints`（最初と最後の時点だけ。年なら最小と最大の年）、CAGR 注記を集合縦棒にも使えるように。

## 2026-09-24 エディタを「画面の高さに収める作業台」にする
- サイドバーを下までスクロールすると、スライドとデータが見えなくなっていた。ページ全体ではなく、サイドバー・スライド・データを別々にスクロールさせる。
- 右側はスライドとデータを上下に分け、境目をドラッグ（矢印キー、ダブルクリックで半々）か「スライドを大きく／半々／データを大きく」で大きさを変える。スライドは欄の中に 16:9 のまま収める（container query）。大きさはこのブラウザに覚える。
- サイドバーの欄は見出しで開け閉めでき（`<details>`）、開閉もこのブラウザに覚える。「表示する行・列」は最初は閉じておく。
- 幅 1000px 以下はページ全体をスクロールし、右側を上に固定して「スライド／データ」のタブで切り替える。
- ③（データと詳細設定）の画面も同じ考え方で作る。

## 2026-09-24 補完パーツを「注記・添え・別の1枚」の3段で整理する
- 一覧（合意済み）：Claude Docs「添えパーツの一覧（Chart Advisor）」 https://claude.ai/code/artifact/7b4b81c0-f4f2-4558-add8-2766bf350ded
- 注記（チャートの中、1点）と添え（軸を揃えて横か下に並べる、2点）。添えは共通の9部品（A CAGRの棒、B CAGRの表、C 最新の構成比、D 構成比の変化、E 揃えた表、F 前年比、G 全体の構成、H 合計の推移、I 示唆ボックス）で、揃える軸は「項目・系列・全体」。
- ③のメニューは部品名ではなく「何を言えるようになるか」でまとめ、そのチャートに合う物はすべて選べる。使えない物はグレーで理由を添える。おすすめは印だけ。
- 7点以上で「詰め込み気味、2枚目に分けますか？」と一言（止めない）。選べなくなるのは添え2つ・枠4つの物理的な上限だけ。
- 作る順：B → E を全チャートに → C → A・D・F・G → H・I。

## 2026-09-24 新しい画面の流れ（① 入り口・② 切り口を選ぶ）を /start に作る
- ① 入り口：A 相談（ルール版の分類 `src/lib/advisor/classify.ts`。AI 版と同じ形を返す）／B 目的（複数選べる）／C チャート。描けるレシピが無い目的・チャートは「準備中」。
- ② 切り口を選ぶ：相談は「推薦1＋他のアングル」（点数の規則で最大3案）、目的・チャートは切り口ごとに「リード案＋サブ案、ほかの候補は折りたたみ」。右の欄で見えにくいことに「＋補完パーツ」か「→別のレシピ」。
- **提案するのは、今のエンジンで描けるレシピだけ**（描けないものは「準備中 N 件」とだけ出す）。部品が増えれば自動で候補に入る。
- 図はレシピのレイアウトから描く抽象図（`RecipeThumb`）。名前・数値は描かない。
- ③ の画面ができるまでは、「次へ」でエディタに移り、サイドバーの「選んだ案」から1枚ずつ作る（`applyRecipe`。データがサンプルのままならレシピに合うサンプルに替え、入れたデータはそのまま）。案ごとのデータ確認（決まった文）もそこに出す。
- 計画はブラウザに保存（`chart-advisor:plan`）。エディタの「② に戻る」で続きから。

## 2026-09-24 プロジェクト＝データ1つ＋スライド N 枚（保存形式 v3）
- 1つのデータから複数の見せ方を作るのが売りなので、エディタの単位を「1枚」から「プロジェクト」に変えた（`src/features/editor/project.ts`）。
- **全スライド共通**：データ・出典・スライドの言語。**スライドごと**：チャート・見出し・設定・補完パーツ・どのレシピから作ったか。
- 画面の部品は今までどおり1枚分の状態を受け取る（`viewOf` / `withView`）。行・列の名前を変えると、ほかのスライドの設定（強調など）も追いかける。チャートを替えたスライドはレシピから外れる。
- ② の「次へ」で、選んだ案がそのままスライドになる（見出しは案の「答える問い」から始める）。スライドの一覧で選んで1枚ずつ調整、複製・削除・並べ替え。
- PPT は全スライドを1回で（最後に元データ）。保存も1回：DB の `spec` 列は1枚目の ViewSpec、`ui` 列にプロジェクト全体。全スライドを検証し、1枚でも壊れていれば保存しない。マイページは1枚目を縮小表示し、「N 枚」を出す。
- v1・v2（1枚だけ）の保存は、読み込み時に1枚のプロジェクトにする。DB の表の作り（プロジェクトと ViewSpec を別の行に分ける）は、必要になった時に見直す。

## 2026-09-24 左右の役割をそろえ、標準構成・任意補完を分ける（指示書19〜25章）
- **左＝文脈・現在地、中央＝成果物、右＝選択・設定・確定**を ② とエディターで共通にした。
  - エディター：左に「今回の伝え方・この構成で答える問い・補完アドバイス」とスライドの一覧（縦）。右に保存・チャート・見出し・見せ方・補完・行と列・データの設定・出力（下に固定）。
  - ②（目的・チャートから）：左は切り口の一覧（押すとその切り口へ移るだけ）。「選んだ案（外す）」「切り口を追加」は右へ。相談からの ② も右の選んだ案に「外す」。
- **② では補完パーツを選ばせない**（今までの「＋ CAGR注記を付けて見せる」をやめ、「エディターで『〇〇』を追加すると補えます」の案内に）。別のレシピを選ぶボタンは残す（伝え方の選択なので）。
- 提案カードは「答える問い → おすすめの構成 → 強み → この案だけでは見えにくいこと＋補い方」の順。
- **標準構成**＝レシピに入っている部品（`standardComplements`）。エディターの「補完」で「標準構成」として最初からオン。外すと「〇〇を外すと、△△が見えにくくなります」と出し、左の「答える問い」を単品レシピの問いに変える（`lostWhenRemoved`・`answeredQuestion`）。
- **任意補完**はレシピに理由付きで持つ（`optional`）。補完の欄は「標準構成／おすすめの補完／ほかにも付けられる」に分けて、そのチャートで描ける物だけ出す。**補完アドバイス**（`advice`）は左に定型文で出し、「右の『補完』を開く」で右へ移るだけ（左では設定を変えない）。
- **追加データが必要な補完**は `optional[].requiresFields` で持ち、② の右で「使いますか？」と確かめる。今は該当する部品が無いので、表示されない。
- データの確認に「推移のレシピなのに行が時間でない」ときの注意を足した。Mekko の形が要るレシピは目的を構成だけにし、推移の切り口に混ざらないようにした。プロジェクトのサンプルデータは1枚目の案に合わせる。

## 2026-09-25 CAGR の表と集合縦棒（開始と終了の比較）

- **CAGR 表**（cagr_table）を実装。系列ごとに最初の年→最後の年の CAGR を計算し、高い順に並べる（計算できない系列は最後）。CAGR のセルは濃さで大小を示す。行が年でなければ「年が必要」と表示する。
- **集合縦棒**（clustered_column）を実装。基準（既定：最初の行、グレー）と比較先（既定：最後の行、紺）を項目ごとに並べ、差の大きい順。増減ラベル（↑ +170）は比較期間のデータが無くても出せる（`baseFreeOn`）。行が年なら項目ごとの CAGR を下に添えられる。
- **レシピから作ったスライドは、レシピの構成のまま描く**。これまでエディタは主チャート1つだけを描いていたため、表や変換（最初と最後の年だけ）が抜けていた。スライドがレシピを持ち、チャートがレシピの主チャートのままなら、レシピのレイアウト・表・変換を使い、主チャートにスライドの設定と補完パーツを載せる。チャートを替えるとレシピの構成は使わない（従来どおり）。Mekko は従来の複合構成のまま。
- レシピの**表も「標準構成」**として右の補完パーツでオン・オフできる。外すと見えにくくなること（例：成長率）を表示し、1枚のチャートのレイアウトに戻す。外した表はスライドの `hiddenParts` に保存。
- これで相談の例（海外5地域の売上）は、指示書どおり「推移＋CAGR表」「開始と終了（集合縦棒）」「規模と構成（最初と最後の積み上げ＋CAGR表）」の3案が描ける。

## 2026-09-25 100%横棒・差分バー・スロープ

- **100%横棒**（bar_100）：行＝棒（レシピでは最初と最後の年だけ）、列＝内訳。内訳は最後の棒の大きい順（「規模の大きい順に並べる」既定オン）。棒の中に構成比、棒と棒の間は内訳の境目を点線で結ぶ。合計ラベル（任意補完）は棒の右。年×系列のデータでも描けるよう、チャート定義に `alsoAccepts: ['MATRIX_TIME_SERIES']` を足した。
- **差分バー**（variance_bar）：基準と比較先（既定：最初の行→最後の行）の差だけを横棒で。差の大きい順、プラスは緑で右、マイナスは赤で左（NarratiX の色）。差の値は棒の先に。見出しは「2021 → 2025 の差」。
- **スロープ**（slope）：最初と最後の2時点（行が年なら最小→最大の年）を線で結ぶ。両端に「系列名 値」を置き、重なる時はラベルだけずらす。強調した系列以外は薄くする。
- これで17レシピのうち「2時点の構成を比べる」「増えた・減ったを分けて見る」「2時点の入れ替わりを見る」も描ける。相談の例の推薦3案は変わらない。

## 2026-09-25 AI 相談の正解表（叩き台）

- AI 版を作る前に、「相談例 → 期待する提案」の正解表を30件用意した（`src/lib/advisor/cases.ts`）。推移10・比較8・構成5・曖昧3・まだ作れない3・英語1。
- 各件に、読み取るべき目的・場面・成長率／規模／正確な値、期待する案（順）、出てもよい案、出してほしくない案、ねらいを持つ。
- 採点（`score.ts`）：目的が正しい・1番目の案が期待どおり・出してほしくない案が出ない、の3つで○。まだ作れない相談は案を出さなければ○、確認を促す相談は確信度が低ければ○。
- ルール版の基準点は **30件中17件**。テストで、この点を下回らないことを確かめる。AI 版は同じ表で採点して比べる。
- 表は Claude Docs「AI相談の正解表（叩き台）」で Tai さんが見直す。直した内容をこのファイルに戻す。

## 2026-09-25 正解表のレビュー反映（段階1）

- 外部レビューを受けて正解表と採点を直した。
  - **行動**を正解に持つ：推薦／確認／未対応。曖昧な相談（V01・V03）と「平均より伸びている」（T04、平均の意味が曖昧）は確認が正解。確認すべき質問も持つ。
  - 成長率・規模・正確な値は「必要・不要・不明」の3つ（表示の「—」をやめた）。
  - T01 の「全体の拡大と構成」は許容へ。C03・C04 は CAGR なしの差の比較を期待、M01 は Mekko を NG（規模のデータが相談に無い）。
- 切り口を2つ追加（19種）：**2つの差を比べる**（集合縦棒＋増減ラベル、CAGR なし。予算対実績・前年対今年）と **1時点の構成を見る**（100%横棒の1本、新しい変換 `latest`）。「開始と終了」は複数年の開始・終了（CAGR 付き）に限定。
- 採点は指標ごと：合格・必須条件（NG なし・未対応を装わない・根拠なく推薦しない）・行動・目的・1番目（厳密／期待内）・上位3件の取りこぼしなさと的確さ・NG・確認・未対応。案は最大3件。
- 縦棒と横棒のように向きだけ違う案は、採点では同じ案とみなす（VARIANTS）。切り口と表示のしかたの分離は段階2で行う。
- ルール版は合格 15/30、必須条件 25/30（テストで下回らないことを確かめる）。
- 30件は開発セット。検証セット（約25件）は Claude が、最終テスト（約20件）は Tai さんかご友人が作る。

## 2026-09-25 分類項目の追加・並べ方の規則・確認の動き（段階3）

- 分類（ConsultationClassification）に項目を追加。AI 版も同じ形で返す。古いデータは既定値（不明）で読む。
  - `expected_action`（推薦／確認／未対応）と `missing_info`（確認で聞くこと：数字・見方・誰に・平均の意味）
  - `time_mode`（1時点／複数時点／2時点）、`comparison_intent`（大小・順位／差／順位の入れ替わり／平均との差）
  - `composition_intent`（構成比／規模と構成比／全体と内訳）、`measure_additivity`（足せる／足せない）、`series_count`（系列が1つ／複数）
- 切り口ごとに合い方（`fit`）を持つ。並べ方の規則：
  - ハード除外：時間の扱いが合わない、足せない指標で積み上げ・構成、系列が1つで複数系列の図、「規模と構成比」でないのに Mekko、「差」でないのに差の図、「構成比」でないのに 100%横棒。
  - 比較・構成の意味が合えば +30、合わなければ −15。成長率が必要なのに見えない切り口は −10、見える切り口は +15。「推移」とあれば期間が無くても時系列として扱う。
  - 向きだけ違う切り口（縦棒・横棒）は並べる時に1つまで（`RECIPE_VARIANTS`）。
- ルール版の分類も直した：「営業利益率」「新規顧客数」で場面を誤らない、「◯◯別」から比べる対象を取る、変化の言葉も期間も無ければ1時点、構成の意味がはっきりしていれば目的は構成、2時点の差・入れ替わりは比較。
- ② に**確認**の画面：案の代わりに決まった質問と選択肢を出し、答えを分類に反映して並べ直す（AI は使わない）。「答えずに案を見る」もある。**未対応**の相談には「まだ作れません。近いチャートを無理に出しません」と出す。
- 結果：開発セット 30/30（開発セットに合わせて直したので当然の満点）。**検証セット（言い換え・境界 25件、`cases-validation.ts`）は 15/25**。検証セットでは規則を調整しない。この差がルール版の限界で、AI 版と比べる基準になる。

## 2026-09-25 データ欄を切り口に合わせる

- **比較期間の表は、使うスライドがある時だけ出す**。使うのは「規模と構成に成長率を添える」、比較期間が要る補完パーツ（Mekko の増減ラベルなど）、Mekko の揃えた表を「期間の伸び」で出す時だけ（`slideUsesBase` / `projectUsesBase`）。使わない時は表1つで、タブも出さない。比較期間にデータが残っていれば「比較期間の表を表示」で開ける（データは消さない）。
- 表の上に「このスライドに必要なデータ」を1行で出す（② のカードと同じ文、`needsText`）。
- データが見本のままなら「今は見本のデータです」と出す。
- **年が列に並んでいたら行と列を入れ替える**：推移のスライドがある時（全部 Mekko でなければ）、表ごと貼り付けた直後に自動で入れ替え、「元に戻す」を出す。手で入力した表で年が列なら注意と「行と列を入れ替える」ボタン。入れ替えボタンは常に置く（`transposeProject`）。入れ替えると、名前を指す設定（強調・比較の対象・表示する行と列）は外す。
- 貼り付けた表の左上の見出しは行の名前、列の名前は空にする（前のデータの名前を残さない）。入れ替えで行が年になり名前が空なら「年」。

## 2026-09-25 フィードバック対応（元に戻す・相性の注意・CAGR 注記・欄の整理・上位だけ表示）

- **元に戻す・やり直す**：プロジェクト全体を履歴で持つ（`history.ts`）。30手まで、700ms 以内の続けた変更は1手、見るスライドを替えただけは数えない。プレビューの上のボタンと ⌘Z／⇧⌘Z（入力欄の中ではブラウザの文字の取り消しに任せる）。ブラウザの控えや保存したチャートを開いた時は履歴を消す。
- **チャートとデータの相性**（`advice.ts`、決まった規則）：行が年なのに Mekko → 積み上げ縦棒、行が年でないのに折れ線 → 縦棒、単位が率・平均・指数なのに積み上げ・構成比 → 折れ線／縦棒、縦棒で項目が13以上 → 横棒ランキング、折れ線が9本以上 → 絞り込み、構成比の図にマイナス。プレビューの上に「◯◯に切り替える」ボタン付きで、左の欄にも出す。
- **積み上げ縦棒の CAGR 注記**：合計（上の注記に「全体」と明記）に加え、終了年の棒の右に系列ごとの CAGR を系列の色で出す（重なればずらす）。
- **データの欄**：「比較する期間／現在の期間」は比較期間を使うスライドがある時だけ。「行・列が表すもの」は見出しに使うと一言添えて残す。
- **上位だけ表示**（設定 `top_n`：すべて／上位3・5・10）：行が年なら最後の年、そうでなければ列の合計で上位を選ぶ。積み上げ・100%・Mekko・横棒ランキング・縦棒比較は残りを「その他」にまとめて最後にグレーで置く。折れ線などと、単位が足せない指標の時はまとめずに上位だけ。CAGR 表も同じ上位にそろえ、「その他」は最後。

## 2026-09-25 要因と関係のチャート（5種）と切り口（6つ）

- **データの形**：要因（DRIVER_BRIDGE）は表の1行目＝始点、最後の行＝終点、あいだ＝要因（1列目の値）。bridge は任意にした。関係（BUBBLE）は行＝項目、1列目＝X、2列目＝Y、3列目＝大きさ。データ欄に役割（始点・要因・終点、X・Y・大きさ）を表示し、合計の列は出さない。
- **要因の計算**（NarratiX の parseDriverBridgeRows_ と同じ）：終点が空なら始点＋要因の合計。合わなければ既定は差を「その他 / 調整」に、「終点を自動補正」なら終点を合計に。値0の要因は設定でオンの時だけ。並び順は影響の大きい順（プラスが先）など6通り。色は標準・モノクロ・高コントラスト（NarratiX の色）。
- **チャート**：ウォーターフォール（始点・終点は濃紺、増加は青、減少は橙、連結線）、要因バー（影響順の横棒）、プラス・マイナスバー（増加要因と減少要因を左右の枠に、同じ尺度）、散布図（相関係数と強さの言葉を注記、中央値で4つに分ける補完）、バブル（半径は大きさの平方根）。見出しの左に「始点 → 終点：差（率）」。散布図・バブルの強調は点（行）から選ぶ。散布図の軸は 0 を含めず、上下に 12% の余白。
- **切り口**：増減を要因に分解する／一番効いた要因を見る／増加要因と減少要因を分けて見る／2つの指標の関係を見る／4つに分けて位置づける／関係と規模を1枚で（計25）。
- **見本データ**：要因＝営業利益の前年からの増減、関係＝製品の市場成長率・営業利益率・売上。見本のまま違う形の目的のチャートに替えると、その目的の見本に替わる。
- **相談**：「要因・寄与・分解」があれば目的は要因、「相関・〜の関係」などがあれば関係。未対応は評価だけ。目的に合う形のデータの切り口に加点し、関係・要因では「成長率」を必要な情報として減点に使わない。正解表の X01・X02、検証セットの L23・L24 を「推薦」に変更（作れるようになったため）。開発 30/30、検証 16/25。

## 2026-09-25 提案が外れた時の立て直し（選び直し・フィードバック・データの形からの提案）

- 試用で「複数の製品の成長率と利益率を見て、投資先を判断したい」に推移が出た（本命はバブル／4つに分けて位置づける）。ルール版は「成長」を拾う。正解表の開発セットに T11 として追加（ルール版は不合格のまま。AI 版で直す想定）。開発 30/31、検証 16/25。
- **どれも違う？（② の選び直し）**：相談をやり直さずに、目的のボタンからその目的の切り口を選べる。AI を使わないので回数を消費しない。未対応の相談でも使える。
- **👍／👎 のフィードバック**：👎 は理由（見せたいことと違う／データの形が合わない／もっと良いチャートがある／説明が分かりにくい）とコメント。「もっと良いチャートがある」を選ぶと選び直しを開く。ログインしている時だけ Supabase の `recommendation_feedback` に保存（相談文・分類・出した案・選んだ案・評価・理由・コメント・レシピ DB の版・分類器）。本人だけ書ける・読める（RLS）。全体はダッシュボードで見る。保存することは画面に明記。**SQL：`20260925000000_recommendation_feedback.sql` を SQL Editor で実行する**。
- **データの形から、ほかの見せ方（エディタ）**：行が項目で数値の列が2つ → 散布図、3つ → バブル、1列にプラスとマイナス → ウォーターフォール、行が年なのに関係・要因 → 折れ線。今のチャートと目的が違う時だけ、プレビューの上（切り替えボタン付き）と左の欄に出す。

## 2026-09-25 散布図・バブルのデータと、形の違うデータを別々に持つ

- **データの形の種類ごとに1つずつデータを持つ**（`ProjectState.datasets`）：表（推移・比較・構成）は `dataset`、要因は `datasets.bridge`、関係は `datasets.relation`。推移とバブルを一緒に選ぶと、以前は1つの表（年×地域）を共有して、バブルが年を点にする変な図になっていた。スライドは自分の形のデータを使い（無ければ見本）、データ欄の見出しは「同じ形の N 枚で共通」。チャートを形の違うものに替えると、その形のデータに替わる（前のデータは残す）。
- **散布図・バブルの表**：左端＝項目、X（横軸）、Y（縦軸）、大きさ（バブルだけ。散布図では「使いません」）、グループ（任意の文字の列）。グループは点の色分けと凡例（NarratiX の色）。貼り付けた表に文字だけの列があれば、それをグループとして読む。行と列の入れ替えは散布図・バブルでは出さない（項目と指標が逆になるため）。
- **軸の名前**：サイドバーの「横軸（X）の名前」「縦軸（Y）の名前」（空なら列の名前）。矢印は付けず、縦軸の名前は軸の上に置く。
- 列が年の表で散布図・バブルを選んだ時は注意（「年ごとの推移なら折れ線」）。「年が列に並んでいます」の注意は表の形のスライドだけ。
- ② の「どれも違う？」の候補を大きなカード（図・問い・強み・構成）にした。

## 2026-09-25 散布図・バブルの設定の整理

- サイドバーの「データ」の欄（単位・行と列が表すもの）は散布図・バブルでは出さない。軸の名前は「横軸（X）の名前」「縦軸（Y）の名前」で決め、単位もそこに書く（単位の注記はスライドに出さない）。
- **X と Y の入れ替え**（設定 `xy_swap`：通常＝1列目が横軸／入れ替え＝2列目が横軸）。表の見出しの「X（横軸）」「Y（縦軸）」も入れ替わる。軸の名前の空欄時の既定も、その軸に来た列の名前。
- **相関係数は既定で出さない**。見せ方の「相関係数を表示」をオンにした時だけ。

## 2026-09-25 2期間の100%積み上げ（カテゴリ別）share_pair を追加
- 社内でよく使う「カテゴリごとに前期・今期の100%棒を並べる」図（例：Versuni の家電カテゴリ別ブランドシェア）を追加。
- データは Mekko と同じ形（行＝カテゴリ、列＝ブランド、比較期間＝前期、現在＝今期）。凡例は右、各棒の上に合計、下に「市場の伸び率」（緑／赤）と「強調したブランドの増減（単位）」。下の2段は見せ方でオフにできる。
- レシピ MIX_PAIR_SHARE（カテゴリごとにシェアの変化を見る）。構成比かつ「増減・前年比」と言われた時だけ出す（requireComparison DELTA）。広く出すと既存の構成レシピを押しのけ、開発セットが 30→27 に下がったため。
- 3期間以上の推移はデータが3次元（カテゴリ×ブランド×年）になるので、今は2時点のみ。

